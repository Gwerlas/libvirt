---
- name: Volumes - Create
  community.libvirt.virt_volume:
    pool: "{{ _libvirt_volume.pool | default('default') }}"
    state: present
    uri: "{{ libvirt_uri }}"
    xml: "{{ lookup('template', 'volume.xml.j2') }}"
